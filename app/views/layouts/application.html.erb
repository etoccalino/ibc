<!DOCTYPE html>
<html>
  <head>
    <title>Ibc</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <link rel="stylesheet" href="http://twitter.github.com/bootstrap/1.4.0/bootstrap.min.css">
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="container-fluid padding-top-baseline">
      <% if flash.any? %>
        <div class="alert-message block-message warning" data-alert="alert">
          <%= link_to "×", '#', :class => 'close' %>
          <ul>
            <% flash.each do |key, value| -%>
              <li><%= h value %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <div>
        <div id="nav" class="indent1">
          <% if current_user %>
            Logged as <strong><%= current_user.username %></strong> - <%= link_to "Logout", :logout %>
          <% end %>
        </div>  
      <%= yield %>
    </div>
  </body>
</html>
